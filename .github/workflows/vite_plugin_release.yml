name: Release

on:
  push:
    tags:
      - 'vite-plugin-v*'

jobs:
  release:
    uses: crxjs/workflows/.github/workflows/release.yml@v1
    with:
      publish: true
    permissions:
      contents: write
      id-token: write
